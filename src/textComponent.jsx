import React, { Component } from 'react'


//conditional rendering of text based on ?url endpoint?
// if endpoint === "localhost:3000/welcome" {
//   do this thing
// } else if endpoint === "localhost:3000/instructions" {
//   do this other thing
// } else if endpoint === "localhost:3000/generate" {
//   do this other thing
// } else if endpoint === "localhost:3000/results" {
//   do this other thing
// } else {
//   break clause
// }

export default TextComponent
